import { View, Text } from "react-native";
import React from "react";
import Icon from "react-native-vector-icons/AntDesign";

const Heart = () => {
  return <Icon name="heart" size={22} color="#CD2424" />;
};

export default Heart;
